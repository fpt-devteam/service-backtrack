# Firebase Configuration
FIREBASE_PROJECT_ID=your-firebase-project-id

# Database Connection Strings
CORE_DB_CONNECTION=Host=localhost;Database=backtrack_core;Username=postgres;Password=your_password
CHAT_DB_CONNECTION=mongodb://localhost:27017/backtrack_chat
NOTIFY_DB_CONNECTION=Host=localhost;Database=backtrack_notifications;Username=postgres;Password=your_password

# Qdrant Vector Database (for semantic search and item matching)
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=  # Optional: set if using authentication

# Note: Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!
